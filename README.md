## Launch App

Run `npm install` for a install dependencies.

## Development server

Run `npm run dev` for dev server. And navigate to `http://localhost:5173/`. The application will automatically reload if you change any of the source files.
